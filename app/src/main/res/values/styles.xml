<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="TextItemTitle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/item_title_text_size</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="TextItemSubTitle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/item_subtitle_text_size</item>
    </style>


</resources>
